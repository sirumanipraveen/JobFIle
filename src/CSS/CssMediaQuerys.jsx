
import './query.css'
function CssMediaQuerys() {
  return (
    <>
    <div>
        <div>
          <h1 className='Heading'>The following example shows a menu that will float to the left of the page if the viewport is 480 pixels wide or wider (if the viewport is less than 480 pixels, the menu will be on top of the content):</h1>
        </div>
        <div>
        <h1 className='Heading'>The following example shows a menu that will float to the left of the page if the viewport is 480 pixels wide or wider (if the viewport is less than 480 pixels, the menu will be on top of the content):</h1>

        </div>
        <div>
        <h1 className='Heading'>The following example shows a menu that will float to the left of the page if the viewport is 480 pixels wide or wider (if the viewport is less than 480 pixels, the menu will be on top of the content):</h1>

        </div>
        <div>
        <h1 className='Heading'>The following example shows a menu that will float to the left of the page if the viewport is 480 pixels wide or wider (if the viewport is less than 480 pixels, the menu will be on top of the content):</h1>

        </div>
        <div>
      <h1 className='Heading'>The following example shows a menu that will float to the left of the page if the viewport is 480 pixels wide or wider (if the viewport is less than 480 pixels, the menu will be on top of the content):</h1>
      </div>
      <div>
      <h1 className='Heading'>The following example shows a menu that will float to the left of the page if the viewport is 480 pixels wide or wider (if the viewport is less than 480 pixels, the menu will be on top of the content):</h1>

      </div>
        
    </div>
    </>
  )
}

export default CssMediaQuerys